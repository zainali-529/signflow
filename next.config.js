/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '*.supabase.co',
        pathname: '/storage/v1/object/public/**',
      },
    ],
  },
  // Required for pdf-lib in server components
  serverExternalPackages: ['pdf-lib'],
}

module.exports = nextConfig
